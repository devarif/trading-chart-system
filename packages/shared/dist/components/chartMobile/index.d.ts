import * as React from 'react';
import { ChartMobileProps } from './types';
/**
 * Primary UI component for user interaction
 */
export declare const ChartMobile: (props: ChartMobileProps) => React.JSX.Element;
